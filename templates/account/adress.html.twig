{% extends 'base.html.twig' %}

{% block title %}Mes adresses{% endblock %}

{% block content %}
<a href="{{path('app_add_address')}}" class="btn btn-dark btn-sm float-right">Ajouter une adresse</a>
<h1>Mes adresses</h1>
C'est dans cet espace que vous allez pouvoir gérer toutes vos adresses.<br/>
<a href="{{path('app_account')}}">Retour</a>
<hr>


{% if app.user.adresses|length == 0 %}
Vous n'avez pas encore ajouté d'adresse dans votre compte client. Pour en ajouter une, Veuillez <a href="{{path('app_add_address')}}" class="">cliquer ici</a>
{% else %}
<div class="d-flex flex-wrap">
{% for address in app.user.adresses %}
<div class="cold-md-4 mr-4 mb-4 mt-4">
<div class="card" style="width: 18rem;">
  <div class="card-body">
    <h5 class="card-title">{{address.name}}</h5>
    <p class="card-text">{{address.adresse}},<br />
    {{address.postal}}-{{address.ville}}-{{address.pays}}
    </p>
   <a href="{{path('app_edit_address',{'id': address.id })}}">Modifier</a> | <a href="{{path('app_delete_address',{'id': address.id })}}">Supprimer</a> 
  </div>
</div>
</div> 
  {% endfor %}
</div>
{% endif %}

{% endblock %} 
